name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: wow-look-at-my/go-toolchain@v1
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_ORG_REPO_READ }}
