name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: wow-look-at-my/go-toolchain@v1
        with:
          private-org-token: ${{ secrets.PRIVATE_ORG_REPO_READ }}
